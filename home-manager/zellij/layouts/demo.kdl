layout {
    default_tab_template {
        children
        pane size=1 borderless=true {
            plugin location="compact-bar"
        }
    }

    tab name="test" {
        pane
        pane command="btop"
        pane {
            command "eza"
        }
        pane command="eza" {
            cwd "/"
        }
    }
}
